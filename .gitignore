node_modules
.next
out
build
*.log
npm-debug.log*

# TypeScript incremental build cache
tsconfig.tsbuildinfo
*.tsbuildinfo

# env — never commit any real env file or backup of one
.env
.env.*
.env*.local
*.env
!.env.example

# prisma / sqlite
prisma/dev.db
prisma/dev.db-journal
*.db
*.db-journal

# local dev image storage (filesystem storage driver)
.local-storage/

# misc
.DS_Store
.vscode
next-env.d.ts

# brainstorming companion (local mockups only)
.superpowers/

# prod data snapshots/backups (contain password hashes — never commit)
prod_backup_*.json
prod_backup_*.sql
.vercel
